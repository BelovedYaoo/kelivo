# 协作记录

- 已硬切本地用户消息为“纯文本正文 + 结构化附件”，发送、临时会话与编辑版本均通过 `ChatService` 完成托管路径校验、流式摘要和原子仓储写入。
- 已移除 `ChatService` 的 marker 解析、启动引用回填、旧路径迁移及写后 best-effort 二次同步；低层仓储历史维护函数不再有生产调用，未改动其表结构以避开主分支并行约束改动。
- 本分支未触碰同步适配器、数据库约束测试、UI 渲染、API 构造或导出逻辑。
- 验证：定向 `flutter analyze` 通过；正文语义 2 项、仓储/版本 22 项、`ChatService` 39 项测试全部通过。
