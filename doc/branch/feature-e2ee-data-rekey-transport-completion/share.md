# 协作摘要

- Issue：#78；基线：`95295ab6`。
- 已完成：`CloudSyncClient` 接入源记录列表、源附件列表、记录暂存、附件暂存和最终提交五个 data-rekey 端点。
- 已完成：请求 DTO 固化租约范围、分页、mutation、版本递增和 v2 完成证明；重复字段由租约或源版本派生。
- 已完成：原始 JSON 严格拒绝未知字段、非法 UUID/Base64URL/UTC/数值、分页乱序与终点错配、暂存回执串线和完成证明篡改。
- 已完成：源记录及 manifest 对随响应返回的密文本地复算 SHA-256；附件分块只校验摘要格式、连续布局和长度，未把服务端元数据误当作已验证分块可用性。
- 验证：定向 `flutter analyze` 通过；14 项 data-rekey 测试通过；同步客户端协议文件 151 项测试通过。
- 边界：未实现执行器、持久日志或附件运行时双代次；完成回执中的 proofFrame/proofDigest 仍需后续执行器结合安全核心与本地成员锚点验证。
