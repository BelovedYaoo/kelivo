# 协作记录

## 当前任务

- 修复 #75：业务写入与附件草稿创建在 SQL 失败时不得发布内存或 UI 幽灵状态。
- 修复 #76：旧扫描结果不得覆盖并发写入后的新状态，也不得丢失唤醒。
- 修复 #77：内容寻址复制成功后仅清理已接管的原受管明文文件，失败保持可恢复。
- 测试边界以 `E2eeChatContentRuntime` 公开接口为主，必要时覆盖 `ChatService` 与数据库公开接口。
- 不修改上传协调器或文件存储的预算与错误分类，不处理远端删除 #52。

## 进度

- 已从 `5147e661` 创建独立修复分支和 worktree。
- #75 已完成：普通消息、开始生成、追加编辑版本均在 SQL 与附件草稿事务成功后才发布缓存和 UI 通知；三条真实回滚用例已由红转绿。
- #76 已完成：附件工作世代阻止旧 `false` 覆盖并发提交；屏障用例验证第二个附件在下一周期推进，无附件写入不增加扫描。
- 正在实现 #77 的受管明文源文件安全收尾与崩溃恢复。
