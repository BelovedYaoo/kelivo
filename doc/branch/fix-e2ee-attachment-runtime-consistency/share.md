# 协作摘要

- #75：附件草稿事务提交成功后才发布消息缓存、计数和 UI 通知，覆盖普通消息、开始生成与追加编辑版本回滚。
- #76：以附件工作世代保护并发唤醒，旧扫描结果不能清除扫描期间提交的新上传工作。
- #77：内容寻址副本与消息、上传草稿、原受管明文淘汰回执同事务提交；资产维护使用 `.kelivo-gc` 隔离、事务内需求复核和崩溃恢复协议，保留外部路径、当前内容寻址文件及仍被引用的源路径。
- #77：运行时启动时仅清理 `upload/e2ee/content` 下无数据库需求的 SHA-256 普通文件；未知文件保留，异常实体失败关闭。
- 验证：8 个改动文件定向 `flutter analyze` 通过；完整数据库目录、ChatService、协议文件存储、内容运行时共 450 项测试通过；`git diff --check` 通过。
- 环境限制：仓库级 `flutter analyze` 仍受既有 #33 阻断；完整 `flutter test` 两次停在 `frontend_server` 聚合编译且未启动 `flutter_tester`，已改用相关目录和文件分组验证。
- 未修改数据库 schema、附件协调器预算或错误分类，未处理远端删除 #52。
