# 协作摘要

- 目标：完成 Issue #84，硬切移除持久明文请求、响应与 Flutter 运行日志，并在业务启动前严格退役旧日志。
- 基线：`feature/e2ee-chat-runtime@07660680`。
- 测试边界：公开启动退役服务，以及网络与日志配置对磁盘的外部行为。
- 验收场景：正常旧日志清理；缺失或空目录；未知文件、reparse/符号链接、删除失败；中断后重启收敛；网络成功、错误、流式响应与应用异常均不落盘敏感内容。
- 约束：不保留兼容读取、开关或静默回退；不记录正文、配置、API Key、凭据、URL query、附件路径；不触碰默认安全槽。
- 已完成：新增严格日志退役器，只接受历史请求/Flutter 活动文件与日期轮转命名；未知条目、异常目录或拓扑变化失败关闭。
- 已完成：安装根、匿名工作区和所有账号工作区先整批验证再耐久删除；五个日志偏好键复用安装级跨账号耐久退役。
- 已验证：退役器 happy、空/缺失、未知条目、异常目录、耐久失败重启收敛共 5 项；真实工作区四目录覆盖 1 项；偏好跨账号覆盖 1 项。
- 集成约束：#85 将提供无任意路径参数的 `KelivoInstallationRootSession.retirePersistentLogs()`；该接口可用后必须删除当前 Dart 路径扫描退役实现，只保留单一无路径调用点。`4314735f` 目前仅作为 writer/数据模型硬切阶段，不构成发布闭环。
- 已完成：删除请求与 Flutter 持久日志写入器；Dio 成功、流式、错误路径均不再记录 URL、头、正文或响应；全局 Flutter 捕获仅保留控制台透传。
- 已完成：删除五项日志设置、关于页隐藏入口、移动/桌面日志查看入口、日志查看/解析页面、存储日志分类与清理操作，并同步四套本地化。
- 已验证：`flutter analyze --no-pub lib` 与新增网络落盘测试定向分析均无问题；网络成功/失败 2 项、设置 6 项、存储与界面 2 项、本机擦除 37 项通过。
- 已知环境噪声：根级 `flutter analyze --no-pub` 被路径依赖缺少开发依赖的 755 项既有错误阻断；并行 #85 修改共享原生资产期间测试构建器提示文件发生变化，测试本身通过，待接口稳定后复验。
- 下一步：接入 #85 的 `KelivoInstallationRootSession.retirePersistentLogs()`，删除临时 Dart 路径扫描实现并完成启动失败关闭集成。
