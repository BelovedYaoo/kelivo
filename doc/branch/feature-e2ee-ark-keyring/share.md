# 协作记录

- 已将 ARK 不透明句柄硬切为最多 8 个代次的密钥环；生成时必须绑定正代次，新增代次严格递增，旧代次可裁剪但当前代次不可删除。
- 设备状态升级为 KDST v2 固定 448 字节，完整密钥环由设备状态密钥认证加密；旧 188 字节状态明确拒绝，不保留迁移路径。
- 账户记录、附件、ARK 信封、注册和配对均按请求代次精确选钥，缺失代次失败关闭且不会回退当前密钥；记录 ID 派生按既定设计使用当前代次。
- 安全核心 ABI 升级到 v10，Dart FFI 只传递不透明句柄，原始 ARK 不跨 ABI。
- 安全审查后将密钥环收紧为固定 8 槽数组；裁剪只在槽间原地覆盖并立即清零源槽，杜绝集合重分配、尾部残留或秘密局部 move。
- 修正后验证已通过：protocol 40 项及文档测试、native 47 项、Dart 依赖 18 项、根项目 2 项精确回归；依赖分析无问题。本轮主实现此前还通过根项目相关回归 363 项。
- 根项目全量 `flutter analyze` 的 737 条中，736 条来自既有 `dependencies/mcp_client/test/**` 缺少 `package:test/test.dart`，另 1 条本次警告已修复。
