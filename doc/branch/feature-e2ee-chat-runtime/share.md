# 协作摘要

- 目标：硬切接通六类聊天实体的本地事务 outbox、加密推送与远端 apply，生产不再使用仅本地同步写入器。
- 跟踪：https://github.com/BelovedYaoo/kelivo/issues/50
- 当前：生产聊天内容运行时、配置 Provider、附件双向断点、schema 19、结构化消息附件与附件 HTTP 已接入；消息密文 payload 已硬切 v2 完整附件身份，上层本地路径 marker 已完全移除。
- 已完成：本地业务写、sync intent 与 dirty 收口已改为同一 SQLCipher 事务；异常整体回滚。
- 已完成：CloudSyncProvider 已建立失败关闭的内容运行时合同，运行时初始化成功后才打开内容门；登出与重启会优先关闭内容运行时并始终清理会话/释放工作区租约。
- 已完成：认证与运行时共用设备状态访问，持久会话硬切 schema v3 并绑定设备密钥版本；ARK 租约严格核对账户、设备与世代后仅允许一次所有权转移。
- 已完成：六类聊天实体支持严格快照读取与事务内远端应用；依赖顺序、墓碑顺序、幂等 turn 删除、无 outbox 回声及拉取生命周期均已有回归门禁。
- 已完成：账户记录信封、会话和安全核心统一接受完整正 uint32 keyEpoch，拒绝零与超界值。
- 已完成：持久账户会话提供唯一认证会话转换入口，Provider 配对批准与后续 transport 不再丢失已验证的 deviceKeyVersion。
- 已完成：SQLCipher schema 17 新增严格配置 Vault 与附件上传断点；十类配置实体支持规范 codec、事务远端应用、依赖排序和墓碑，旧数据库 schema 不迁移。
- 已完成：生产聊天内容运行时组装 ARK、记录加密、outbox、pull、聊天适配器与串行调度器；有效会话的 ChatService 不再使用 LocalOnly 写入器。
- 已完成：八个配置 Provider 在账户态从 SQLCipher Vault 水合；本地 Provider、Vault 与 outbox 原子提交并可回滚，远端刷新不产生 outbox 回声，旧 SharedPreferences 不再作为同步配置真源。
- 已完成：安全核心 ABI v8 提供不透明附件密钥、ARK 包装及绑定上传上下文的独立分块加解密；原始 ADK 不进入 Dart。
- 已完成：附件 manifest 使用 ARK 认证的账户记录密文，密文内绑定 attachmentId、uploadId、keyEpoch、完整分块布局、内容摘要、包装 ADK、显示名称和 MIME；外层身份错配、篡改、截断及非法边界均失败关闭。
- 已完成：附件创建、manifest、分块与提交四阶段持久状态机落入 SQLCipher；mutationId、精确待发密文路径、CAS 租约、过期接管与失败重试跨重启保持一致，4 MiB 分块密文不进入数据库或 WAL。
- 已完成：附件 create、put、commit、manifest、chunk、delete 六个强类型 POST 操作接入完整会话令牌；请求与响应严格绑定 attachmentId、uploadId、keyEpoch、分块身份和长度，拒绝额外响应字段及非规范 Base64URL。
- 已完成：消息密文 payload 硬切 v2，附件引用必须包含规范 attachmentId、uploadId、正 uint32 keyEpoch、kind 与 order；旧 v1 和缺失远程身份的旧结构一律拒绝。
- 已完成：单消息密文附件上限固定为 32，编码与解码均在递归复制附件对象图前失败关闭；正式 schema 继续约束连续 order 与远端身份唯一性。
- 已完成：schema 18 持久化上传分块 SHA-256、连续失败与终止原因，并新增完整身份幂等的下载断点、暂存重建、精确 CAS 清理及资产注册原子提交；终止状态保留全部清理凭据。
- 已完成：附件资产 GC 会同步清除已删除本地资产的下载映射；同一内容允许多个远端附件身份复用，schema 17 仍由安装门禁硬切且不迁移。
- 已完成：附件原子文件存储使用账户工作区固定布局、同目录独占临时文件、持久性屏障与无覆盖发布；上传/下载 staging 隔离，最终内容按 SHA-256 寻址，只有单个密文分块允许整体进入内存。
- 已完成：上传 staging 路径硬切绑定分块 mutationId，消除随机 AEAD 密文在“文件发布后、DB 记账前”崩溃后的重建冲突；内容寻址文件支持受限分块读取与全量流式校验。
- 已完成：schema 19 将资产、消息有序附件引用与 GC 状态纳入正式 Drift/SQLCipher 结构；远端身份三元组必须完整，同一消息的 attachmentId/uploadId 不得重复，同一本地资产允许建立多个独立远端上传身份，旧 schema 不迁移且运行时不再动态补表。
- 已完成：下载明文暂存按 attachmentId/uploadId/keyEpoch 隔离为单一 plaintext.part，以数据库确认偏移恢复和截断；发布前流式核对长度与 SHA-256，并以内容摘要原子发布。
- 已完成：附件上传协调器按持久草稿有界推进 create、manifest、chunk 与 commit，支持精确 mutation 重放、租约接管、源文件一次认证和失败关闭。
- 已完成：上传草稿与 `message_asset_rows(revision_id, ordinal)` 建立唯一复合外键；创建前严格核对本地资产与消息元数据，commit 在同一事务内回填远端三元组并完成上传租约 CAS，任一步失败整体回滚。
- 已完成：下载协调器在 pull 事务外按远端步骤预算物化并认证附件，事务内只接受精确 ready 身份；pending 与未来 keyEpoch 均保持整页 checkpoint 不变，空附件也会原子清理旧引用。
- 已完成：`ChatMessage` 持有最多 32 个不可变有序结构化附件，仓储所有消息写入口原子写入资产与引用并批量水合；同步 apply 将 ready 附件直接交给消息仓储，避免先删后写的双事务窗口。
- 已完成：本地发送、临时会话与用户消息编辑已直接构造结构化附件，正文保持纯文本；托管路径、普通文件、流式摘要和 32 项上限均在事务前失败关闭，消息与引用单事务落库。
- 已完成：生产层已移除本地路径 marker 解析、启动回填、沙箱路径迁移及二次 best-effort 引用同步；仓储中无调用的旧迁移/回填 API 与专用测试已删除，不保留旧明文路径兼容入口。
- 已完成：聊天展示、mini map、模型请求与导出均直接消费 `ChatMessage.attachments`；本地旧 marker 仅作为普通文本，远程 HTTP/HTTPS/data 内联图片保持原有语义，导出不泄漏本地绝对路径。
- 已完成：模型请求管线以进程内强类型字段传递结构化附件并在预处理阶段强制移除，不再把本地路径编码为临时正文 marker；字段损坏时失败关闭，附件为空正文仍可正常进入请求。
- 已完成：Android/iOS 云同步页面可切换登录与首设备注册并提交 displayName，桌面端保持只能登录；请求期间禁止重复提交，四份 ARB 与生成本地化已同步。
- 已完成：安全核心 ABI v9 暴露通用 336 字节 ARK 轮换信封 seal/open，完整绑定账户、签发设备、目标设备、公钥与 keyEpoch；ARK 只以不透明句柄存在，失败输出清零并受关闭/并发门禁保护。
- 已完成：同步调度器将不可重试的 unauthenticated/forbidden 提升为单次终止事件，解除在途周期后再通知 Provider；Provider 以会话世代隔离旧回调，先持久清除会话，再关闭 runtime/ARK 并释放工作区租约。
- 已完成：ARK 不透明句柄硬切为最多 8 个严格递增代次的固定密钥环，设备状态硬切 KDST v2 固定 448 字节；历史记录、附件和信封按精确代次选钥，缺失代次失败关闭且不回退当前密钥。
- 已完成：密钥环裁剪使用固定槽原地覆盖并立即清零源槽，不再使用可能遗留秘密副本的 Vec 重分配或 remove；主线复验通过安全核心 18 项及账户工作区/同步协议 216 项测试。
- 已完成：平台无关的 E2EE 单次后台同步运行器统一核算单调总时限、网络步骤和附件密文字节预算；截止或取消会中止并等待在途网络结算，结束后解除取消监听，并按内容运行时、账户租约、工作区租约逆序释放所有权。
- 已完成：后台运行器使用无 UI Provider 的配置桥接和单周期内容运行时；不可重试的 401/403 会先持久清除会话再完整清理资源，构造失败保留首要异常且不会泄漏网络客户端或业务租约。主线定向分析通过，数据库约束与内容门禁 148 项测试通过。
- 已完成：安全核心 ABI v11 新增账户信任根 Ed25519 派生、签名与严格验签；ARK 注册表和 Dart 句柄硬绑定规范 userId，所有密钥环合并、记录、附件、配对和信封路径拒绝跨账户复用，原始 ARK 与派生 seed 不跨 FFI。主线依赖 19 项、根项目四组 282 项测试及两侧静态分析通过。
- 已完成：SQLCipher schema 21 新增每账户唯一的已验证成员清单锚点，清单强制至少一名成员并符合固定 wire 步长，同时约束摘要、security generation、完整 uint32 keyEpoch、CAS 版本与时间；冻结 schema 仅保留 v21，旧库不迁移。
- 已完成：成员清单锚点只接受私有构造的已验证清单，持久化重验入口与数据库命令位于同一 Dart library 私有边界；安装和推进均以写语句取得首个事务快照，双连接竞争收敛为幂等成功或明确陈旧。真实 SQLCipher 关闭重开、撤销轮换、跨连接 capability、版本耗尽和篡改均已覆盖，数据库 94 项、协议 120 项测试通过。
- 已完成：成员清单 wire 固定当前信任公钥、上一完整摘要、恢复公钥与 capsule 摘要；轮换旧/新 epoch 双签，初始化与新增设备过渡签名为零，自撤销失败关闭。公开恢复 genesis 字节入口已删除，必须等待原生恢复介质 capability。
- 已完成：记录 ID 派生强制使用记录自身 keyEpoch；与成员清单安全核心合并后统一 ABI 13，避免两份 ABI 12 动态库错误互认。native 49、protocol 41+doctest、依赖 20、根协议 120 项测试通过。
- 已完成：iOS 安全密钥槽硬切 Keychain `AfterFirstUnlockThisDeviceOnly`、Data Protection 且禁止同步，无文件或偏好回退；Runner/CocoaPods 最低 iOS 14，ActivityKit 扩展保持 16.1。ABI 13 下 native 49、protocol 41+5 doctest、依赖 21、根协议 120 项通过，三个 Apple Rust target 的 check 与严格 Clippy 通过。
- 已完成：`kelivo-api/main@02dce9e` 已推送 D1/R2 密文附件六端点、原子配额、幂等状态机及完整 uint32 keyEpoch；未部署。
- 已完成：安全核心 ABI v15 使用独立恢复口令加密固定 644 字节恢复介质，二维码与文件共享同一密文字节协议；恢复私钥与 ARK 不进入 Dart，Argon2 64 MiB 工作区及可控 KDF 中间对象显式清零，历史总量以 16 MiB 双层门禁限制移动端工作集。
- 已完成：恢复导入在单个 Native 调用内验证完整成员历史、打开当前及必要的相邻源 epoch capsule，并原子发布不透明 ARK keyring；生产恢复能力仅由 Android/iOS 声明，Windows ABI 对生成、导出和执行恢复失败关闭，同时保留普通 capsule 密封能力。
- 已完成：Dart 成员清单支持 op4 恢复接续与 op5 恢复替换，严格验证完整历史 operationId 唯一性、成员差分、旧/新 ARK 双签名与恢复状态约束；Dart 生成的 INIT→RESUME→RESUME→REPLACE 固定 wire 已由 Rust 逐段验摘要、签名和转换。
- 已确认：成员清单必须签名覆盖恢复公钥、恢复胶囊版本与摘要，并在 SQLCipher 持久化最后验证清单锚点；服务器投影只参与 CAS，不能成为信任来源。普通登录使用的会话代必须与签名成员的稳定认证代分离。
- 已确认：恢复介质必须固定 genesis 完整清单；清单显式携带当前信任公钥，并以旧 epoch 过渡签名和当前 epoch 持有签名建立不可伪造的完整历史链，否则恶意服务器可向恢复设备替换为自选 ARK。撤销禁止由被撤销设备自发起；单一恶意服务器仍可隐藏或回放真实旧前缀，但不能构造服务器已知 ARK 的有效后继。
- 已确认：ARK 轮换后必须冻结普通同步并完成全局 data-rekey；记录按精确 epoch 派生 ID，附件拆分分块 epoch 与 manifest epoch，服务端游标绑定 data generation，最终新 snapshot 完成后才可幂等裁剪旧 ARK。保证撤销后的新写前向隔离，不声称让旧设备忘记其已获得的历史明文或密钥。
- 已完成：客户端会话硬切 v4，稳定安全控制面接入当前状态、连续历史与设备轮换；注册和配对必须先以本地 ARK 验证并安装 schema 21 成员锚点，确认恢复事务并持久化无 bootstrap 会话后才可打开内容网络。
- 已完成：前台与无 UI 后台共用安全引导提交顺序；移动后台生产 Runner 已接入真实 SQLCipher、设备状态与成员锚点门禁，同时保留总截止、取消、迟到资源所有权和有界清理。根项目定向分析、生成客户端分析及 322 项联合回归通过。
- 已完成：服务端 data-rekey completion proof 已硬切 v2 并推送但未部署；Flutter 生成客户端已从该 OpenAPI 重新生成，冻结快照、成员清单摘要、暂存集合摘要和双终点游标模型保持一致。生成包与根项目分析、协议 128 项测试通过。
- 已完成：客户端 data-rekey v2 已接入严格状态、租约、源记录/附件分页、记录/附件暂存与最终提交五端点；固定 270 字节完成证明只能由当前设备身份 Ed25519 签名并严格验签。主分支协议 152 项、protocol 60+5 项、native 66 项及依赖 Flutter 27 项测试通过，#78 已关闭。
- 进行中：服务端重加密保留 chunkKeyEpoch 与 R2 分块、仅提升 manifestKeyEpoch；客户端正在硬切拆分 manifestKeyEpoch、chunkKeyEpoch 与 manifestRevision，普通上传初始两代相等，不保留兼容路径。该项完成后才能实现 data-rekey 持久执行器。
- 已完成：匿名 LocalVault 的 Windows 安装根边界、Windows 安全槽 reparse 路径竞态及附件上传取消/终止认证/本地截止语义已合入主 E2EE 分支；370 项附件与运行时测试、64 项原生测试、25 项安全核心 Flutter 测试及定向静态分析通过，#72/#73/#74 已关闭。
- 已完成：附件草稿事务提交后发布、上传扫描世代唤醒与受管明文源持久回收已合入；主分支数据库、ChatService、同步协议与内容运行时 493 项测试通过，#75/#76/#77 已关闭。
- 已完成：服务端冻结 rotation issuer 作为唯一 data-rekey 协调者，仓储层原子阻止越权换租和暂存清空，并在 manifest revision 耗尽时拒绝轮换；`kelivo-api/main@1cd4a19` 已推送，181 项测试、类型检查和 Workers dry-run 通过，未部署。
- 已完成：客户端 data-rekey 规范字节核心统一 source、staged、270 字节 completion、proofDigest 与本地密文 SHA-256；设备状态持久化新增绑定 locator、generation、slot 与完整帧摘要的跨进程原子 CAS。根分支分别通过 15 项协议测试、21 项设备状态测试及 253 项同步/Provider 回归。
- 已完成：首设备恢复安全引导固定绑定 `https://kelivo.bemylover.top`，独立恢复口令全路径清零，只有原生导出的 644 字节密文在持久化明确确认后交付注册；根 9 项与安全核心 27 项测试通过。移动端二维码/文件 UI 和服务端恢复挑战仍未接线。
- 已完成：公开前硬切删除 WebDAV/S3 明文远端备份、自动提醒和对应 UI；启动在工作区偏好前缀绑定后、业务数据库与 UI 启动前严格清理当前工作区旧凭据、调度键与临时明文包，只保留用户主动选择目标的本地导入导出。#82 启动回归已修复，非当前账号命名空间的完整退役由 #83 跟踪。
- 已完成：安全核心将配对、信任、附件与恢复路径的单块 HKDF 中间 PRK/T(1) 纳入可控清零所有权；native 66、protocol 61、依赖 Flutter 27 项及两侧严格 Clippy/分析通过，#70 已关闭。
- 安全阻塞：#81 已确认原生全槽删除测试误触 Windows 默认安全槽，既有槽可能已被删除；当前 `08d79f...` 槽已由配对取消红测登录名与创建时间精确解释为测试残留，不是旧密钥恢复证据。修复测试级临时 SlotStore 隔离并审查 canary 前，禁止运行任何可能访问默认槽的原生或 Flutter 集成测试。
- 复审阻塞：ABI17 三提交已合入并通过 native 71/71，但 `cfg(test)` 隔离不覆盖 Dart 加载的生产库；另有 Android 目录换链 TOCTOU、Unicode 文件名 panic 与 Windows 跨进程换槽竞态。#81 继续冻结，原生分支正在补第二轮修复。
- 已完成：ABI18 本机密码学擦除已接入安装级租约、耐久 marker、全槽删除与 Windows/Android 原生安装根擦除；Linux/macOS/iOS 明确 Unsupported，生产自撤销在 #51 committer 接入前保持禁用。
- 已完成：Android 隔离 AVD 已验证全部已注册工作区明文备份凭据精确删除、未知命名空间零删除失败关闭及强停重启耐久性，#83 可关闭；fresh Android 安全槽创建回归由 #90 独立修复。
- 已完成：data-rekey schema 24 持久日志、账户记录相邻世代重包、纯字节身份校验及租约边界已合入；完整执行器和旧 ARK 裁剪继续由 #51 推进。
- 已完成：账户恢复客户端已接入 proof 前冻结历史、加密 checkpoint、授权响应丢失恢复及 resume/replacement 持久请求与回执协调；Native ABI19、两轮 data-rekey 和移动端入口仍在推进。
- 已完成：`kelivo-api/main@0820c9c` 已合入账户恢复服务端与 D1 Free 限额状态机，230 项测试通过，op4 终态不超过 45 次查询、op5 为 47/50；未部署、未操作远端 D1/R2。
- 下一步：完成 ABI19 恢复 proof/成员材料、#51 撤销与 data-rekey 执行器、Android #90、移动端恢复入口，再执行 Windows/Android/Linux 发布门禁与全新 D1/R2 硬切。
- 已知边界：Native 恢复证明、撤销后的 data-rekey 执行器、移动恢复业务入口、Android 真机后台时序及生产部署尚未闭环，因此当前内容同步仍不能作为完整公开 E2EE 发布。
- 约束：不保留旧数据、旧 payload 或双写兼容路径；业务接线完成并验证前不提前开启内容同步。
- 已完成：附件双代次、相邻代重包限制、明文 staging 清理、连续换代候选和高水位边界均已合入；执行器启用仍等待 #51 完整状态机。
- 已完成：Windows 设备状态跨 isolate 互斥改为共享打开、禁止删除并由阻塞式 `LockFileEx` 持有独占字节锁，不再依赖跨 FFI 的线程局部错误值；真实 CAS、写写、写读删竞争及账户工作区 115 项回归通过。
- 已验证：云同步当前设备撤销、幂等回执、并发登出、Provider 生命周期及本机擦除 UI 门禁 13 项定向测试通过；测试未访问默认安全槽。并行原生构建期间出现 native assets 文件时间戳变化提示，待代理结束后无并发复验。
- 进行中：ABI19 已以红测锁定恢复 challenge、proof transcript、nonceProof、跨设备绑定及截断/保留位失败关闭；采用 Native opaque execution session，恢复私钥、ARK 与 nonce 不进入 Dart。
- 新阻塞：服务端成员清单禁止设备自签 op3 撤销自己，且自撤销后立即吊销普通会话；允许当前设备自行轮换会使它必然获知新 ARK，不能成立“撤销后失去访问”的安全保证。API #40 采用保守方向：当前设备只提交固定 mutationId 的移除请求并使用窄用途状态查询，另一台仍可信设备签发 op3 并完成 data-rekey；最后设备必须走恢复替换，等待产品语义确认。
- 进行中：#84 持久明文请求/运行日志硬切、#85 受管根句柄删除能力、#49 移动恢复 UI 与 #51 data-rekey 执行器并行推进；不部署、不操作远端 D1/R2。
- 已清理：账户恢复 API/D1 四个已整合工作树与分支，以及客户端账户恢复、data-rekey 基础、本机擦除、移动恢复导出状态和旧 ABI18 工作树与分支；残留编译器/长路径清理由既有 #44/#55 覆盖。
- 已完成：Android 首次安全槽创建根因修复已合入 `fe61b5cf`；系统托管 data 根只在 Java 边界规范化，Rust 以 `O_PATH` 穿越不可读祖先且应用可写后代继续 nofollow。隔离 API 36 AVD、跨 PID 解封、native 75 项、账号工作区 115 项、Android Debug APK 与根分支静态复验通过，#90 已关闭。
- 复审阻塞：账户恢复 ABI19 在 `keyEpoch > 1` 且 data-rekey 已 ready 时错误省略最近轮换前驱 capsule，导致完成一次轮换后的后续恢复必然在 proof 前失败；恢复分支正补 rotated-ready 红测与精确历史选择，修复前不集成。
- 复审阻塞：#85 将 Android `/data/user/0` 固定视为符号链接，但现代 AOSP/API 36 使用与 `/data/data` 同身份的 bind mount，当前实现会使合法安装根打开失败并阻断启动；正补系统别名身份验证与隔离 AVD 回归，修复前 #84/#85 均不集成。
- 复审阻塞：Apple 耐久偏好尚缺 headless isolate 注册，且无类型 JSON 会丢失积分 Double 类型；绝对路径重解析、rename 后目录耐久 barrier、写前尺寸门禁、PrivacyInfo reason 与 SwiftPM FlutterFramework 依赖也需修复。#86 在 Apple 编译、XCTest、模拟器和真机验证前保持打开。
- 复审阻塞：服务端 #40 自撤销分支缺主体 intent 签名、pending 权限收窄与有界请求，终态非单调；另有 op4 交错回执、D1 单 SQL 100 参数、token 指纹和 OpenAPI 窄令牌合同问题。分支已冻结并按红测修复，未部署、未操作远端 D1/R2。
- 同步发布门禁：服务端 #40 已将成员清单硬切 v2（header 260 字节，offset 224 新增 32 字节 operationAuthorizationDigest，memberCount 移至 offset 256）；客户端 Dart 与 Rust 仍是 v1。#51 负责 Dart v2 及 op3 intent 绑定，#49 负责 Rust 历史解析与 op4/op5 builder；两端固定向量一致前，API 与客户端都不可发布。
- 已合入：`df137e28` 合并 Apple 耐久偏好安全擦除，包含固定 Library 根身份、异常路径复核、后台门禁和四语恢复提示；Windows 可验证的路径依赖分析、5 项单测及根定向分析通过，Swift/XCTest、iOS 14+ 真机与 macOS APFS 仍是发布门禁。
- 原子集成门禁：恢复 manifest/media v2 协议提交 `41c95466` 已条件通过独立审查，但必须与 App v2 约束 `e6972d8a` 及最终 ABI20 同批合入；单独发布会让旧 ABI19 动态库绕过版本门禁。
- 恢复事务模型：同一源换钥允许连续 `recoverResume` 接管，G+2 只是第一次接管；完成凭据必须验证源换钥到最新恢复 head 的完整连续同 epoch 链，并绑定最新 issuer、generation 与 manifest digest。最终 replacement 必须使用独立第二 challenge，禁止复用首次 challenge。
- 进行中：#92 以 recovery execution 专用原子 ABI 生成 source、双代状态和未激活 target-only candidate；完成 proof 后认证同一候选才允许裁剪旧 epoch。checkpoint 必须在远端提交前持久化三态并显式推进 candidatePrepared -> proofVerified -> activated。
- 已验证：`kelivo-api@c2972b2` 消除 D1 256 目标时的大 `IN` 与超 100 bind 语句，独立复审实测整批 34 条 SQL、单条最大 99 bind、250 项测试和类型检查通过；连续接管回执与第二 replacement challenge 仍在实现，未部署。
- 非阻塞清理：仓库基线已跟踪 4 个 Android/iOS `build/**` 产物，Issue #93 负责从索引删除并补齐忽略规则；当前 E2EE 提交未新增或修改这些文件。
- 2026-08-02 继续集成：按 API 自撤销/二阶段恢复 -> App 成员清单与恢复介质 v2/换钥执行器 -> Native 恢复 execution -> 持久明文退役的顺序原子合入；最终 OpenAPI typed adapter、固定向量和 ABI 门禁全部通过前不部署、不访问远端 D1/R2，也不运行可能访问默认安全槽的旧集成测试。
