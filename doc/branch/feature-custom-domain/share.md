# feature/custom-domain 协作记录

- 目标：将客户端默认云同步服务地址硬切换为 `https://kelivo.bemylover.top`。
- 当前进度：客户端默认地址与主分支协作摘要已完成硬切换。
- 兼容边界：不保留旧域兼容或同步作用域迁移；已有登录会话需退出云同步后使用新地址重新登录。
- 验证结果：两个相关测试文件共 26 项通过，`flutter analyze` 无问题。
