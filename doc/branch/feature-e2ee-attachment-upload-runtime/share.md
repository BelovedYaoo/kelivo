# 协作记录

- 已将持久附件上传协调器接入生产 E2EE 内容运行时：本地内容寻址落盘、上传草稿和消息 outbox 同事务提交，远端 commit 回填完整身份后才允许消息封装发送。
- 上传 create、chunk、commit 复用持久 mutation，统一计入单周期网络、密文字节、截止和取消预算；无附件消息不进入附件准备或数据库扫描路径。
- 上传草稿按 `(targetRevisionId, targetOrdinal)` 自然键幂等恢复；来源字段不一致明确冲突，既有终止状态原样返回。
- 消息密文只包含远端附件身份、类型和顺序，旧 marker、本地路径、摘要、名称和 MIME 均不会进入消息记录。
- Issue #52 已覆盖本任务，未重复创建。相关四个完整测试文件通过；13 个变更文件定向分析通过。全仓分析仍受既有 `dependencies/mcp_client` 缺少 `package:test` 阻断。
