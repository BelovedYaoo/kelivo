# 协作摘要

- 基线：`e1e97478`；独立 worktree `D:\Projects\Private\_worktrees\kelivo-recovery-workspace-preparation`。
- 已新增恢复专用工作区租约，只暴露规范账户作用域、工作区键与数据目录；准备过程不写会话或注册表，也不切换当前工作区。
- 账户作用域由正式会话与恢复准备共用同一规范构造函数，非规范服务地址或 UUID 失败关闭。
- runtime 在首个异步等待前占用工作区变更权；准备中或租约活动时拒绝重入、绑定、退出及关闭，租约幂等关闭后恢复操作。
- 明文状态退役纳入同一变更权：活动恢复租约时拒绝清理，退役进行中拒绝恢复准备与拓扑枚举，成功或异常退出均释放占位。
- 已覆盖作用域与路径一致、关闭后幂等准备、当前账号及注册表不变、非法输入、异步失败释放占位和并发操作拒绝场景。
- 最终代码通过定向 `dart analyze`；租约调整前的四项 Flutter 定向测试均通过，调整后因统一预览构建窗口要求未再执行 Flutter 或原生命令。
