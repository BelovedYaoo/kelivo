# 协作摘要

- 目标：完成 #42 移动可信设备批准配对时的 op2 签名成员清单提交接线。
- 基线：`feature/e2ee-chat-runtime@82065bd9`。
- 完成：移动端批准前读取并验证本地成员锚点与服务端当前状态，使用既有账户信任模块签发并自验证 op2，再提交 generation/digest CAS。
- 完成：签发者设备材料与已验证锚点精确匹配；目标设备绑定二维码公钥、密钥版本及激活后 `authGeneration=1`。
- 完成：私有 capability 在网络批准前后复核同一锚点；批准阶段不安装或推进本地锚点，后续由通用成员历史同步收敛。
- 完成：生产 Provider 注入协调器，数据库路径与内容运行时使用同一入口；公开 Snapshot/Plan 已删除，外部不能伪造 prepared 提交。
- 验证：变更文件定向分析零问题；`flutter analyze lib test` 零问题；配对提交协议边界与移动扫码批准两个精确测试通过。
- 环境边界：仓库根 `flutter analyze` 受未改动路径依赖缺少 `test`/`pigeon` 开发依赖影响；未运行全量测试或真实 FFI 槽测试，遵守 #81 安全槽冻结。
- 剩余：真实 Android/Windows 端到端扫码批准与消费、签发端消费后的通用成员历史推进仍需后续验收；本分支不部署、不关闭 #42。
