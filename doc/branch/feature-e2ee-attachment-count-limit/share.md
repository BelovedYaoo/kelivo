# E2EE 单消息附件数量上限

- 基线：`d1b49677`；范围仅限 sync payload v2 codec 与现有协议测试。
- 完成：导出单消息附件上限 32；编码和解码均在递归冻结前拒绝超限数组，正式 schema 校验仍保留相同门禁以及连续 order、附件与上传身份唯一性规则。
- 覆盖：32 条完整引用往返成功；33 条编码、解码失败关闭；长度专用列表和超深解码元素证明超限时不会先复制附件对象图。
- 验证：目标 `flutter analyze` 无问题；新增测试 2/2；完整协议测试 98/98。
